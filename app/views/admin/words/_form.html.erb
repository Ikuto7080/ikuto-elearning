<%= form_for [:admin, :category, @word] do |f| %>
    <%= render "error_messages"%>
        <div class="row">
            <div class="field">
                <%= f.label :Word %><br>
                <%= f.text_field :name %>
            </div>


            <span class="btn-group pull-right">
                <%= f.submit "Save"%>
            </span>
        </div>   
    </div>
<% end %>