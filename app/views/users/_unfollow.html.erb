<%= form_for(current_user.actvive_relationships.find_by(followed_id: @user.id),
            html: {method: :delte},
            remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "btn btn-large btn-secondary" %>
<% end %>